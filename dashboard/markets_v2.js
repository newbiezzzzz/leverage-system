const markets={BTCUSDT:{name:'Bitcoin / USDT',type:'Crypto',next:'Acquire history and pass 250-row validation'},XAUUSDT:{name:'Gold / USDT',type:'Gold',next:'Verify public-data provenance and ingest'},CL:{name:'WTI Crude Oil',type:'Futures',next:'Verify continuous-contract and roll handling'},FCPO:{name:'FCPO Palm Oil',type:'Futures',next:'Find zero-cost verifiable history'}};const param=new URLSearchParams(location.search).get('instrument')?.toUpperCase();const state=id=>id==='BTCUSDT'?['READY','PENDING','PENDING','LOCKED','LOCKED','NOT TRADEABLE']:['PENDING','NOT TESTED','NOT TESTED','LOCKED','LOCKED','NOT TRADEABLE'];function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function card(id,m){const s=state(id),labels=['Data','Strategy','Backtest','OOS','Paper','Tradeable'];return `<article class="card panel market-detail-card"><div class="panel-head"><div><p class="eyebrow">${id}</p><h2>${m.name}</h2></div><span class="pill blue-pill">${m.type}</span></div><div class="metrics"><div><b>${s[0]}</b><span>data</span></div><div><b>${s[1]}</b><span>strategy</span></div><div><b>${s[2]}</b><span>backtest</span></div><div><b>${s[5]}</b><span>trade readiness</span></div></div><div class="market-gates">${labels.map((x,i)=>`<div class="gate-row ${s[i]==='READY'?'gate-ready':s[i]==='PENDING'?'gate-pending':'gate-locked'}"><span>${i+1}</span><strong>${x}</strong><b>${s[i]}</b></div>`).join('')}</div><div class="insight"><span>NEXT ACTION</span><div><strong>${esc(m.next)}</strong><p>This market is not tradeable until the evidence gates pass.</p></div></div></article>`}function boot(){const target=param&&markets[param]?param:null;document.getElementById('title').textContent=target?markets[target].name:'Choose your market';const ids=target?[target]:Object.keys(markets);document.getElementById('marketCards').innerHTML=ids.map(id=>card(id,markets[id])).join('');if(target){document.getElementById('detail').style.display='block';document.getElementById('detail').innerHTML=`<div class="panel-head"><div><p class="eyebrow">${target} CUSTOMER PATH</p><h2>Data → Strategy → Backtest → OOS → Paper → Tradeable</h2></div></div><p class="muted">A blocked gate prevents the market from being presented as ready.</p>`}}boot();