async function getJson(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(`${path} ${r.status}`);return r.json()}
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function money(v,currency='RM'){return `${currency} ${Number(v||0).toFixed(2)}`}
function projectNumber(projects,id){const i=projects.findIndex(p=>p.id===id);return i<0?'P-???':`P-${String(i+1).padStart(3,'0')}`}
function statusClass(v){const s=String(v||'').toLowerCase();return ['active','online','ready','verified','connected'].includes(s)?'status-good':(['paused','waiting','not_verified','unknown','not_connected'].includes(s)?'status-warn':'status-off')}
function chips(items){return (items||[]).length?(items||[]).map(x=>`<span class="pill2">${esc(x)}</span>`).join(' '):'<span class="muted2">None recorded</span>'}
async function render(){const id=new URLSearchParams(location.search).get('id');const root=document.getElementById('detail');if(!id){root.innerHTML='<section class="section"><h2>Project not specified</h2><p class="muted2">Return to the project registry and select a project.</p></section>';return}try{const [projects,types,metrics,snapshot]=await Promise.all([getJson('/api/projects'),getJson('https://raw.githubusercontent.com/newbiezzzzz/leverage-system/main/control_plane/project_types.json'),getJson('https://raw.githubusercontent.com/newbiezzzzz/leverage-system/main/control_plane/project_metrics.json'),getJson('/api/snapshot')]);const p=(projects.projects||[]).find(x=>x.id===id);if(!p){root.innerHTML='<section class="section"><h2>Project not found</h2><p class="muted2">The registry does not contain this project.</p></section>';return}const info=types?.types?.[p.type]||{};const m=metrics?.projects?.[id]||{};const t=m.traffic||{};const f=m.funnel||{};const r=m.revenue||{};let gates=null;try{gates=(await getJson(`/api/projects/${encodeURIComponent(id)}/gates`)).report}catch{}const num=projectNumber(projects.projects||[],id);const nextGate=gates?.current_gate?.label||p.next_gate||'Not recorded';root.innerHTML=`<section class="detail-hero"><div><div class="project-id">${num} · ${esc(p.id)}</div><h1>${esc(p.name)}</h1><p class="muted2">${esc(p.description||'No description')}</p></div><div><span class="pill2 ${statusClass(p.status)}">${esc(String(p.status||'UNKNOWN').toUpperCase())}</span><span class="pill2 ${statusClass(p.lifecycle_stage)}">${esc(String(p.lifecycle_stage||'UNKNOWN').toUpperCase())}</span></div></section><section class="section"><div class="grid3"><div class="metric-box"><span>Status</span><strong>${esc(String(p.status||'unknown').toUpperCase())}</strong><small class="muted2">Project operating state</small></div><div class="metric-box"><span>Revenue</span><strong>RM ${Number(p.verified_revenue_usd||0).toFixed(2)} USD</strong><small class="muted2">Verified revenue only</small></div><div class="metric-box"><span>Capital</span><strong>${money(p.capital_deployed||0)}</strong><small class="muted2">Owner spend tracked</small></div></div></section><section class="grid2"><section class="section"><h2>Project identity</h2><div class="kv"><b>Project number</b><span>${num}</span><b>Project ID</b><span>${esc(p.id)}</span><b>Type</b><span>${esc(info.label||p.type||'Unclassified')}</span><b>Stage</b><span>${esc(p.lifecycle_stage||'Unknown')}</span><b>Revenue status</b><span class="${statusClass(p.revenue_status)}">${esc(p.revenue_status||'Unknown')}</span><b>Created</b><span>${esc(p.created_at||'Not recorded')}</span><b>Published</b><span>${esc(p.published_at||'Not published')}</span></div></section><section class="section"><h2>Commercial channel</h2><div class="kv"><b>Primary channel</b><span>${esc(p.store||info.channels?.join(', ')||'Not assigned')}</span><b>Live URL</b><span>${p.store_url?`<a href="${esc(p.store_url)}" target="_blank" rel="noopener">${esc(p.store_url)}</a>`:'Not recorded'}</span><b>Price</b><span>${p.price_usd?`US$${Number(p.price_usd).toFixed(2)}`:'Free / usage monetization'}</span><b>Sales verified</b><span>${Number(p.verified_sales||f.verified_sales||0)}</span><b>Next gate</b><span>${esc(nextGate)}</span></div></section></section><section class="section"><h2>What Leverage is operating</h2><div class="kv"><b>Channels</b><span>${chips(info.channels)}</span><b>Delivery</b><span>${chips(info.delivery)}</span><b>Build capabilities</b><span>${chips(info.build_capabilities)}</span><b>Revenue events</b><span>${chips(info.revenue_events)}</span><b>Production policy</b><span>RM0-first · no local-PC production · owner approval for consequential actions</span></div></section><section class="section"><h2>Traffic & acquisition</h2><div class="grid3"><div class="metric-box"><span>Traffic status</span><strong class="${statusClass(t.status)}">${esc(String(t.status||'NOT CONNECTED').toUpperCase())}</strong><small class="muted2">Never treat unknown as zero</small></div><div class="metric-box"><span>Visitors</span><strong>${t.unique_visitors==null?'UNKNOWN':esc(t.unique_visitors)}</strong><small class="muted2">Verified measurement only</small></div><div class="metric-box"><span>Conversion</span><strong>${f.conversion_rate==null?'UNKNOWN':`${Number(f.conversion_rate).toFixed(2)}%`}</strong><small class="muted2">Calculated only with verified inputs</small></div></div><div class="kv" style="margin-top:16px"><b>Measurement source</b><span>${esc(t.measurement_source||'Not connected')}</span><b>Last verified</b><span>${esc(t.last_verified_at||'Not recorded')}</span><b>Traffic sources</b><span>${(t.sources||[]).length?(t.sources||[]).map(x=>esc(`${x.name||x.source}: ${x.metric||'metric'}`)).join(' · '):'None recorded'}</span></div></section><section class="section"><h2>Income & economics</h2><div class="grid3"><div class="metric-box"><span>Verified revenue</span><strong>USD ${Number(r.verified_revenue_usd||p.verified_revenue_usd||0).toFixed(2)}</strong><small class="muted2">Authoritative evidence only</small></div><div class="metric-box"><span>Verified sales</span><strong>${Number(f.verified_sales||p.verified_sales||0)}</strong><small class="muted2">Confirmed transactions only</small></div><div class="metric-box"><span>Capital deployed</span><strong>RM ${Number(r.capital_deployed_rm||p.capital_deployed||0).toFixed(2)}</strong><small class="muted2">Must remain RM0 for current experiment</small></div></div></section><section class="grid2"><section class="section"><h2>Workers / technology</h2><ul class="list"><li>Research Worker — demand and SEO opportunity research</li><li>Code Worker — static tool generation, tests and deployment-ready changes</li><li>Measurement Worker — traffic evidence preparation</li><li>Operations Worker — deployment/health checks</li><li>Finance Worker — revenue verification and reconciliation</li><li>Acquisition Worker — organic traffic/acquisition planning under policy guardrails</li></ul></section><section class="section"><h2>Quota & cost controls</h2><ul class="list"><li>Owner capital: RM0</li><li>Local PC production: disabled</li><li>Paid acquisition: prohibited without explicit approval</li><li>Unknown traffic: never converted to zero</li><li>Revenue: recorded only after authoritative evidence</li><li>Money movement: protected / approval-gated</li></ul></section></section><section class="section"><h2>Current blockers & next actions</h2><div class="kv"><b>Blocker</b><span>Google Search Console ownership verification is not yet complete.</span><b>Next action</b><span>Install Google's exact verification file, verify ownership, submit sitemap, then measure indexed pages and organic traffic.</span><b>Current state</b><span>Website live at <a href="https://leverage-tools.pages.dev/" target="_blank" rel="noopener">https://leverage-tools.pages.dev/</a>; revenue still RM0 verified.</span></div><p class="danger muted2">Leverage must not claim income from traffic, clicks, rankings or tool usage. Income is counted only after an authoritative payout/sale event.</p></section>`}catch(e){root.innerHTML=`<section class="section"><h2>Project detail unavailable</h2><p class="muted2">${esc(e.message||e)}</p></section>`}}
render();