name: strategy-hunter-autoloop-legacy

on:
  workflow_dispatch:

jobs:
  legacy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy autoloop retained for history. Leverage V2 continuation is controlled by leverage-supervisor.yml."
