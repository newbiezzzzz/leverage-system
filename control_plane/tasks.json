{
  "version": 2,
  "tasks": [],
  "statuses": ["queued", "running", "blocked", "completed", "failed", "cancelled"],
  "task_contract": {
    "required_fields": ["id", "project", "worker", "description", "status"],
    "recommended_fields": ["action", "target", "priority", "created_at", "due_at"],
    "action_rule": "action must match a registered worker capability or be a controlled report/plan/route action",
    "safety_rule": "restricted actions remain blocked without owner approval and enabled external integration"
  }
}
